import Grid2 from "@mui/material/Unstable_Grid2/Grid2";

const Home = () => {
  return (
    <Grid2
      sx={{
        display: "flex",
        flexDirection: "column",
        gap: "1.5rem",
        alignItems: "center",
      }}
    ></Grid2>
  );
};

export default Home;
